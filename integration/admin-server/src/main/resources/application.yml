server:
  port: 8080

spring:
  profiles:
    active: dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000 # authorization-server's port
    client:
      registration:
        message
